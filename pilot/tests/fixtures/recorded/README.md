# Recorded fixtures

Generated by `tests/recorded_scenarios.rs` via the `run_or_replay`
helper. Re-record with `PILOT_RECORD=<substring> cargo test
--features test-support`. See `pilot::test_support::recorded_test`.
