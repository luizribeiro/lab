//! `rfl chat` surface area. Currently houses only the startup-ordering
//! test seam (c02 / pi-3 M-4 / pi-5 B-2); the orchestration body still
//! lives in [`crate::run_chat`].

#[doc(hidden)]
pub mod test_ordering_hook;
