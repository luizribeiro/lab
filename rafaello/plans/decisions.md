# Architecture decision log

Append-only. Reversals add a new row that references the reversed
decision; do not delete or edit prior rows.

Status values:

- `proposed` — written into `overview.md` and pending owner ratification. The default state for newly-added rows during the design phase.
- `locked` — committed, irreversible without re-opening v1 scope. Reserved for decisions the owner has explicitly locked.
- `ratified` — agreed by owner + claude + pi after debate. Requires the owner's sign-off on convergence (`plans/README.md` §Workflow Phase 1).
- `deferred-to-vN` — not in v1; revisit at the named version.
- `reversed` — superseded by a later row (see Reverses column).

| # | Date | Decision | Rationale | Status | Reverses |
|---|------|----------|-----------|--------|----------|
| 1  | 2026-05-07 | No embedded scripting language in v1; declarative TOML+Markdown plus subprocess plugins. | `streams/c-scripting/rfc-scripting-decision.md` — every motivation collapses into declarative config, and the cases that aren't (custom tools, loop, renderers) are exactly where lockin isolation pays. Reversible later by adding Luau; shipping then removing would cost users their init files. | ratified | — |
| 2  | 2026-05-07 | Lockin is the v1 sandbox; capsa is v2/v3. | Lockin is shipping today; capsa is research. The manifest schema is identical for both, so the swap is mechanical. | ratified | — |
| 3  | 2026-05-07 | Bus = broker (in core) + transport (fittings). Two layers, not one. | Avoids cross-stream confusion and keeps fittings free of topic/ACL/taint concepts. `overview.md` §4.1. | ratified | — |
| 4  | 2026-05-07 | Single canonical topic grammar + four namespaces (`core.*`, `provider.<provider-id>.*`, `plugin.<topic-id>.*`, `frontend.<attach-id>.*`). | Prevents publish-authority confusion; pinned in `streams/a-security/rfc-security-model.md` §5.1–5.2. The three id types have distinct lifetimes (overview §4.3). | ratified | — |
| 5  | 2026-05-07 | Topic-id form is `id_<base32(sha256(canonical))[0..16]>` with collision rejection at install. | Replaces the un-collision-safe `_`-substitution from the round-1 draft (pi-A round-2 finding 7). | ratified | — |
| 6  | 2026-05-07 | Provider plugins publish on `provider.<provider-id>.*`; core re-emits canonical `core.*` events. | Keeps `core.*` core-only (pi-A round-1 finding 2); CaMeL fits as a normal provider plugin without special-casing. | ratified | — |
| 7  | 2026-05-07 | Mandatory taint on `core.session.tool_request` and `core.session.tool_result`; structured `{source, detail}`; populated by core, not plugins. | Closes the verbatim-exfil case at the bus, not at plugin boundary. Pi-A round-2 finding 6/8 resolved by §7.2 of the security RFC. | ratified | — |
| 8  | 2026-05-07 | Mandatory `in_reply_to` on tool_result, RPC reply, confirm_answer, provider tool_request, and provider assistant_message. | Removes the "omit the field to strip taint" escape (pi-A round-2 finding 6). | ratified | — |
| 9  | 2026-05-07 | Bus-level sink confirmation, core-mediated, fail-closed. **Any sink call without matching `user_grants` requires confirmation, taint-independent.** | Tainting is provenance, not authorisation; this is the conservative rule pinned in `overview.md` §6.2. Reverses an earlier "non-user taint AND sink" formulation that survives in security RFC §10 (to be retro-patched). | ratified | — |
| 10 | 2026-05-07 | User-only taint is provenance, not authorisation; `user_grants` is the only confirmation bypass. | Pasted secrets and API keys in user prompts must not authorise themselves to flow to network sinks (pi-A round-2 finding 5). | ratified | — |
| 11 | 2026-05-07 | Trifecta graph check is one-hop direct, not transitive. | Cross-tool flows are caught at the bus (decision 9), not by per-plugin trifecta; the per-plugin check is therefore deliberately conservative-but-shallow. Security RFC §7.1.1. | ratified | — |
| 12 | 2026-05-07 | Carve-outs by compile-time decomposition; loud `--allow-credential-paths` override. | Lockin lacks deny-subpath precedence; decomposition is sufficient for v1 without an upstream lockin feature. Security RFC §7.3. | ratified | — |
| 13 | 2026-05-07 | Bus authentication = inherited socketpair fd (`RFL_BUS_FD`); no UDS path, no token. | Forge-proof inside the sandbox; lockin-compatible (pi-A round-1 finding 1); identical primitive transfers to capsa vsock. | ratified | — |
| 14 | 2026-05-07 | Helper plugins are a v1 primitive (`bindings.helper_for`, `RFL_HELPER_FD`). | Unblocks clean CaMeL Q-LLM isolation; generalises to any sandboxed sub-task (pi-A round-2 finding 2). Security RFC §7.4.1. | reversed | row 26 |
| 15 | 2026-05-07 | Frontends are first-class bus principals (`frontend.<attach-id>.*`); UDS+token attach for external; **frontends are trusted UI principals, not lockin-sandboxed**. | Frontends speak for the user (answer confirmations); confining them as plugins would either break the trust model or make the TUI useless. Security RFC §5.7; overview §3, §10. | ratified | — |
| 16 | 2026-05-07 | Per-plugin private state at `${PROJECT_ROOT}/.rafaello-plugin-data/<plugin-id>/` is automatic, no grant; excluded from `has_workspace_write`. | Otherwise every plugin trips the trifecta (pi-A round-2 finding 8). Security RFC §7.5. | ratified | — |
| 17 | 2026-05-07 | Capability scoped bundles accepted, flattened at compile time; per-method enforcement happens in core, not in lockin. | Lockin can't switch policies live; v2 may tighten when it can. Manifest RFC §11 #1 → resolved here. | ratified | — |
| 18 | 2026-05-07 | fittings v1: `Request.id: Option<JsonRpcId>`, `Response.id: JsonRpcId`; two channels (unbounded response, bounded notification) with drop-on-full. | Pi-B round-2 findings 1, 2, 3 resolved by `streams/b-fittings/rfc-fittings-notifications.md` §2a, §3a, §3b. | ratified | — |
| 19 | 2026-05-07 | RenderTree is purely semantic (no colour/layout); core downgrades unsupported nodes to `Unknown { fallback }` server-side. | Keeps frontends dumb; matches Stream E §6. | ratified | — |
| 20 | 2026-05-07 | Streaming entry topics live under `core.session.entry.*`. | Stream E's unprefixed `session.entry.*` violates the namespace ACL; core is the publisher. Overview §4.2 reconciliation. | ratified | — |
| 21 | 2026-05-07 | The default LiteLLM provider ships as a bundled `rfl-litellm` subprocess plugin, not as built-in core code. | Preserves "providers ship as plugins" goal and uniform trust model (pi review 1 finding 4). Overview §8.1. | ratified | — |
| 22 | 2026-05-07 | Bidirectional `PeerHandle` (notify + call, both directions over one fd) is promoted from fittings "Future work" to a v1 requirement. | Bus broker fan-out cannot be implemented from `ServiceContext::notify` alone, and `renderer.render` / helper-spawn / future tool-call modes need core→plugin request/response (pi review 1 finding 1; pi review 2 blocking finding 1). Overview §15.6. Supersedes the round-1 `ServerHandle::notify`-only proposal. | ratified | — |
| 23 | 2026-05-07 | Bus payload schemas are owned by Stream A; JSON-RPC envelopes are owned by Stream B. | Pi review 1 finding 6: prevents divergent schemas. Overview §15.2; security RFC §9 #3 retro-patched. | ratified | — |
| 24 | 2026-05-07 | Eager-plugin handshake failure is fail-closed in v1; only override is `rfl plugin start --skip-eager`; no manifest knob. | Pi review 1 finding 5.3: removes the speculative `load.eager_failure` knob. Manifest knob deferred to v2. | ratified | — |
| 25 | 2026-05-07 | Manifest filename canonical: `rafaello.toml`. | Harmonises Stream A (was `plugin.toml`) and Stream F (already `rafaello.toml`). Pi review 1 finding 5.2. | ratified | — |
| 26 | 2026-05-08 | **Helper plugins (`bindings.helper_for`, `RFL_HELPER_FD`) deferred to v2.** | The only v1 consumer would have been CaMeL Q-LLM isolation, but CaMeL is itself v2. Forward-compat cost is near-zero — adding `helper_for` later is purely additive. Frees overview §9 work, Stream F bindings, install-flow surfacing. Owner-approved deferral 2026-05-08. | ratified | reverses #14 |
| 27 | 2026-05-08 | **External UDS-attached frontend principals deferred to v2.** v1 ships TUI only, local-spawned, with the `frontend.<attach-id>.*` namespace reserved. | Single-frontend single-instance is sufficient for the v1 dogfooding loop. Drops attach socket auth, token issuance, multi-frontend negotiation, and `frontend.hello` capability handshake from v1 scope. Owner-approved 2026-05-08. | ratified | partially reverses #15 (TUI-as-bus-principal kept; external attach deferred) |
| 28 | 2026-05-08 | **Streaming entry patch ops (`stream_state: "open"` / `"patch"`) deferred to v2.** v1 emits `stream_state: "final"` only — turn-by-turn replies. | Removes the patch op vocabulary, redrawable-vs-append-only branching, coalescing logic. Cost is the user-visible "wait for full reply" UX; mitigated by v1 being a research/dogfooding cut. `final`-only is already in the schema; adding `open`/`patch` later is purely additive. Owner-approved 2026-05-08. | ratified | partially reverses #20 (topic spelling kept; patch op states deferred) |
| 29 | 2026-05-08 | **Subprocess plugin renderers deferred to v2.** v1 renderers are built-in Rust, registered at compile time; the `renderer.render` request/response method on plugin renderer servers is not implemented. Plugin authors who want custom rendering wait for v2. | Removes the renderer subprocess protocol, daemon-side renderer cache, and `peer.call` to a renderer plugin from the v1 surface. Built-in kinds (`text`, `code_block`, `tool_call`, `tool_result`, `error`, `heading`, `thinking`, `image`) cover the dogfooding loop. Surfaces a hard v1 commitment for plugin authors so it shouldn't hide. Owner-approved 2026-05-08. | ratified | partially reverses #19 (RenderTree shape + server-side downgrade kept; subprocess renderers deferred) |
| 30 | 2026-05-08 | **Manifest omits `runtime` in v1.** Implicit default is subprocess + lockin. Field returns when capsa lands. | One less knob in the v1 manifest schema; capsa is v2/v3 (decision #2). Forward-compat: introducing `runtime` later defaults to current behaviour for v1 manifests. Owner-approved 2026-05-08. | ratified | refines #2 |
| 31 | 2026-05-08 | **Manifest `[rpc]` block dropped; plugins ship a path-referenced `openrpc.json` sibling instead.** v1 requires the file to be present at the manifest's parent directory; install-time validates. | Decouples manifest from fittings' specific schema representation. Cleaner than re-stating method signatures in TOML. Schema-introspection-by-running-the-plugin is better ergonomics but circular dep on fittings; defer that to v2. Owner-approved 2026-05-08. | ratified | — |
| 32 | 2026-05-08 | **Manifest compiles to lockin's Rust builder API at spawn time, not to a `lockin.toml` artifact.** | Eliminates a serde round-trip and the failure modes that come with it; type-safety from manifest field → builder method; faster cold start. Verified against lockin's existing public API surface. Owner-approved 2026-05-08. | ratified | refines #2 |
| 33 | 2026-05-08 | **Milestone branches accumulate on `rafaello-v0.1` and merge to `main` only at v1 completion.** | Owner-chosen branch model. Keeps `main` at the rafaello-design merge until v1 is demo-ready. `plans/README.md` updated to match. Optional: m0's fittings work could split out as a separate `fittings-v0.X` merge to `main` if a fittings consumer needs it before v1, but default is to stay on the integration branch. | ratified | — |
| 34 | 2026-05-08 | **Public `rfl serve` deferred to v2.** v1 has no daemon command; `rfl chat` runs the agent core and the TUI together in one process tree, persisting sessions to SQLite under `${PROJECT_ROOT}/.rafaello/state/`. | With external UDS frontend attach deferred (row 27), there is no v1 consumer of an attach socket; defining `rfl serve` as a public command without a frontend to attach is more confusing than helpful. The internal `core` lifecycle still exists as a library — `rfl chat` invokes it directly — so the v2 add is just exposing the existing process to an external TUI/web client. Drops attach socket auth, token issuance, and multi-frontend negotiation from v1. Pi review 2 must-fix #4. | ratified | — |
| 35 | 2026-05-08 | **fittings cancellation builder is `Server::with_cancellation(method: &str, id_field: &str)`** — a single method taking the cancellation method name and the JSON `params` field name to read the request id from. Supersedes the RFC text in `streams/b-fittings/rfc-fittings-notifications.md:251-256` and `:951-952`, which described two separate builder methods (`with_cancellation_method` + `with_cancellation_id_extractor` accepting a closure `|params| -> JsonRpcId`). | Field-name lookup is sufficient for both the LSP default (`$/cancelRequest` + `id`) and the MCP override (`notifications/cancelled` + `requestId`); no v1 consumer needs the closure form. Ratified in `commits.md` c17 at commit-plan time. The closure form is strictly additive if a future consumer needs it. Per `plans/README.md` §"Authoring conventions" stream RFCs are not retroactively rewritten; this row is the canonical record. m0 retrospective §2.1. | ratified | — |
| 36 | 2026-05-08 | **`FittingsError::MethodNotFound` typed `method: Option<String>` field deferred to m1.** The landed shape is `MethodNotFound { message: String, data: Option<Value> }` — the same shape as the other four predefined variants. The RFC (`streams/b-fittings/rfc-fittings-errors.md:71-79`, `:202`, `:239-244`) requires a typed `method` field plus encoder/decoder logic that synthesises `data.method` from it on the wire. | Scope §W2 lists the five predefined variants as carrying `data` + `message` only; the typed `method` field was not in the m0 in-scope item set. Adding `method: Option<String>` to a public Rust enum struct variant later is **source-breaking**, not purely additive — exhaustive pattern matches over `FittingsError::MethodNotFound { .. }` need updating only if they bind fields by name (`{ message, data }` keeps compiling, `{ message, data: _ } if matches!(..)` style does not), and direct struct-literal constructors (`MethodNotFound { message, data }`) break unconditionally. Acceptable to defer because fittings has not yet hit a public v1 stability boundary; the cutover lands cleanly in m1 alongside any other RFC↔scope reconciliation. Callers needing the method name in m0 can read `data.method` *if* the producer chose to populate it — the current `FittingsError::method_not_found(msg)` constructor path leaves `data: None`, so producers have to opt in. Surfaced by pi review of m0 retrospective (finding #2); framing corrected in pi round-2 review. | ratified | — |
| 37 | 2026-05-08 | **Per-plugin private state path key is the topic-id form, not the canonical id.** `${PROJECT_ROOT}/.rafaello-plugin-data/<topic-id>/` where `<topic-id> = id_<base32-no-pad-lower(sha256(canonical-id))[0..16]>` per row 5. Refines row 16, which used the unqualified `<plugin-id>` (ambiguous between canonical-id and topic-id forms). | The canonical id `<source>:<name>@<version>` contains `:`, `/`, and `@` and is not a safe filesystem segment. The topic-id form is path-safe by construction and was already used for bus topics per row 5. m1's `compile::compile_plugin` injects the topic-id form (scope §C5; `rafaello/crates/rafaello-core/tests/compile_private_state_grant.rs`). Surfaced by pi-4 finding 4 of m1 scope review; m1 retrospective §2.5 lands the doc fix. | ratified | refines #16 |
| 38 | 2026-05-08 | **The bundled default provider plugin is `rfl-openai`, speaking the OpenAI Chat Completions wire protocol.** Provider id is `"openai"`; namespace `provider.openai.*`. The plugin contains no LiteLLM-specific code paths. The endpoint URL, the `env.pass` env-var name (e.g. `OPENAI_API_KEY` or `LITELLM_API_KEY`), and `network.allow_hosts` are install-time configuration in the lock — *what* OpenAI-compatible endpoint the user points the plugin at (OpenAI's API, a LiteLLM proxy, vLLM, a local stub) is a deployment detail, not the plugin's identity. Refines row 21, which named the plugin `rfl-litellm` and treated LiteLLM as a distinct provider type. The `plans/README.md` tooling-notes endpoint (`https://litellm.thepromisedlan.club/v1`, `LITELLM_API_KEY`) is the dev environment's *configuration choice*, recorded as a default for `rfl init`'s materialised lock — not baked into plugin source. | LiteLLM is an OpenAI-compatible proxy; "litellm" is the deployment, not the wire protocol. The plugin code targets the OpenAI Chat Completions API and is identical regardless of which OpenAI-compatible endpoint backs it. Naming the plugin after the deployment confused the plugin's identity with one possible host. Owner-flagged 2026-05-08 during a docs audit; refines row 21 rather than reverses, because row 21's "bundled-but-not-built-in" architectural decision still holds — only the plugin's name and identity change. m5/m6 milestone descriptions and overview §8.1 / §15.1 / §16 update accordingly in the same commit batch. | ratified | refines #21 |
| 39 | 2026-05-10 | **m2 supervisor refuses to spawn lock entries with `bindings.provider = true`.** `PluginSupervisor::spawn` returns `SpawnError::InvalidPlan { reason: ProviderNotInM2 { provider_id: String } }` when m1's compiled `BrokerAcl::PluginAcl.provider_id.is_some()`. The refusal is a deliberate staged restriction in v1's m2 cut: m2 ships the broker + supervisor but does NOT yet wire `provider.<provider-id>.*` publish authority (per scope §"Out of scope" / pi-2 §11). m4 removes this refusal when it ships the provider role. | Without this refusal, a provider plugin would spawn with `bindings.provider = true` but the broker would silently reject every `provider.<id>.*` publish as `PublishOnReservedNamespace` (since m2's broker treats that namespace as reserved to-be-defined-later). Failing fast at spawn with a typed reason is a cleaner UX than a successful spawn that produces zero useful publishes. m2 retrospective §2.1 records this; m4's scope owns removing it. Surfaced by pi review-2 finding 11 of m2 scope review. | ratified | — |
| 40 | 2026-05-10 | **m1's reserved env-var list extended in m2 c04** to include `RFL_HELPER_FD`, `RFL_TOPIC_ID`, `RFL_PROJECT_ROOT`, `RFL_PRIVATE_STATE_DIR` alongside the existing `RFL_BUS_FD` / `RFL_PLUGIN`. m1's `scrubber.rs` `RESERVED_ENV_VARS` now rejects all six at compile/V3 time when present in `env.set` / `env.pass`. | The four new names are env vars m2's supervisor injects at spawn time per scope §SP4 step 12 + §F5: `RFL_HELPER_FD` (reserved-by-design even though helpers are deferred per row 26 — defence in depth so a hand-mutated plan can't smuggle a fake helper fd), `RFL_TOPIC_ID` (the hashed plugin-id segment per row 37 — fixture uses it for own-namespace publishes), `RFL_PROJECT_ROOT` (parent of `.rafaello-plugin-data`), `RFL_PRIVATE_STATE_DIR` (the per-plugin write grant). Without scrubber-level rejection, plan authors could collide with these names and either shadow them at spawn time or surface confusing failures. m2 retrospective §2.5 records the back-reach to m1; the change is additive (existing m1 plans without these names continue to compile). | ratified | refines #16, #37 |
| 41 | 2026-05-10 | **Replay carries on `core.session.entry.finalized` with `replay: bool` envelope flag, not a separate `core.session.entry.replay` topic.** The wire shape is `{entry, tree, seq, replay: bool}`; `replay = true` for history events emitted by `SessionController::replay_history`, `replay = false` for fresh events emitted by `SessionController::finalize_entry`. m3 c23 + c30 land the canonical wiring. | Round-2 m3 scope review collapsed away from a separate `entry.replay` topic because (1) the broker fan-out to a frontend ACL with `subscribe_patterns = ["core.session.**"]` already routes both classes of event without ACL changes, (2) the TUI's renderer code path is identical for replay and live (only the empty-buffer-vs-append decision changes, which is determined by seq-monotonicity, not topic identity), and (3) splitting topics would force every consumer to subscribe to two patterns. The `metadata`-on-`EntryMetadata` alternative was rejected because `replay` is a transport-level routing fact (does the consumer treat this as backfill?) not entry content (which is identical across replay/live). m3 retrospective §2.4. | ratified | — |
| 42 | 2026-05-10 | **`BrokerError` grows `FrontendNotInAcl`, `FrontendAlreadyRegistered`, `FrontendNotRegistered`** for m3's frontend-registration surface; **existing `PublishOutsideGrant` and `InvalidInReplyTo` reshape to take `publisher: Publisher` rather than `canonical: CanonicalId`** so the same variant covers core / plugin / frontend publishers. The reshape is source-breaking inside `rafaello-core` but contained — m3 c14 lands the variants and migrates the m2 broker tests in the same commit. | The frontend-registration ACL (rows reflected in Stream A's m3-banner extension) needs typed errors symmetric to plugin registration. The `Publisher`-shaped reshape is a one-time API improvement: m4 / m5 will introduce a `Provider` publisher variant; without the reshape, every new publisher class would need parallel `PublishOutsideGrant{Plugin,Frontend,Provider}` variants, which is uglier than one variant whose payload enumerates the publisher kind. m3 retrospective §2.5. | ratified | refines #23 |
| 43 | 2026-05-11 | **`BusEvent.request_id: Option<JsonRpcId>` is mandatory at the broker for inbound publishes whose topic suffix is `.tool_request`, `.tool_result`, `.assistant_message`, or `.user_message`.** The field is `Option` only because m2 non-correlation-bearing events (`core.lifecycle.*`, `core.session.entry.finalized`, etc.) are still served the same `BusEvent` shape with `None`. m4 c07 (`2bbf3e7`) lands the workspace cutover; per-handler enforcement is proven by `broker_plugin_tool_result_missing_request_id_rejected.rs`, `broker_publish_core_session_user_message_missing_request_id_rejected.rs`, `broker_provider_tool_request_missing_request_id_rejected.rs`, `broker_frontend_user_message_missing_request_id_rejected.rs`. | The provider re-emit pipeline (m4 §C re-emit) needs every cross-plugin envelope to carry a JSON-RPC id so tool dispatch can correlate `tool_result` back to the originating `tool_request` without relying on payload-level conventions. Making the field required only on the four correlation-bearing topic suffixes (rather than globally) preserves the m2 lifecycle/finalize event shapes without a second `BusEvent` variant. Surfaced by m4 scope pi-5 finding L-1; landed in c07 as a single workspace cutover per the m0 c08 unsplittable-cutover precedent. m4 retrospective §2.3. | ratified | — |
| 44 | 2026-05-11 | **`Publisher::Provider { canonical, provider_id: String }` and `PublisherIdentity::Provider { canonical, provider_id: String, topic_id: String }` land as live wire-schema variants in m4.** The c07 (`2bbf3e7`) workspace cutover extends both enums in the same commit as the `request_id` rollout (row 43). `Publisher::Provider` flows through `BrokerError::PublishOutsideGrant` / `InvalidInReplyTo` symmetric to the `Plugin` / `Frontend` arms (refines row 42's reshape rationale); `PublisherIdentity::Provider` carries the third `topic_id` segment on the wire symmetric to the m2 `Plugin {canonical, topic_id}` shape, asserted by the c07 schema test `bus_event_serializes_provider_publisher_identity.rs`. | m4 promotes the previously-reserved `Provider` variant from "named in Stream A's m3 banner as reserved" to "on the wire". Bundling it into the c07 cutover avoids a second workspace-wide constructor-site sweep and matches the m3 `Frontend` precedent. The active provider plan is selected **by canonical id** (`lock.session.provider_active: Option<String>` is parsed via `CanonicalId::parse` and looked up in `compiled_plugins` by `rfl chat`). The `provider_id` is a separate identifier — the public-namespace segment carried in the `provider.<id>.*` topic prefix and the wire `PublisherIdentity::Provider.provider_id` field; it is populated from `entry.bindings.provider_id` into `PluginAcl.provider_id` by `broker_acl::compile`. Provider topics live in the public `provider.<provider-id>.*` namespace while canonical ids identify installed packages, so subscribers must see both; the two identifiers serve different roles and are not interchangeable. The `topic_id` segment on `PublisherIdentity` matches the published topic's third segment and is parallel to the m2 `Plugin` identity shape. m4 retrospective §2.2. | ratified | refines #42 |
| 45 | 2026-05-11 | **The live m1 manifest schema for the `load` field is the string shorthand `load = "eager"` (and `load = "lazy"`), not the table form `[load] eager = true`.** The table form `[load]` with `triggers.kind = "tool"` (etc.) remains reserved for event-triggered lazy loading per m1's manifest RFC, but the simple eager-vs-lazy choice is expressed as a string. m4's two new fixture manifests (`rafaello/fixtures/rafaello-mockprovider/rafaello.toml`, `rafaello/fixtures/rafaello-readfile/rafaello.toml`) use the string shorthand. | `scope.md` round 6 §C8 (and the Stream F manifest RFC examples) carry the obsolete `[load] eager = true` table syntax from a m1-era draft; the live m1 `validate_*` path accepts only the string shorthand for the eager/lazy choice. The c20 / c22 driver pre-corrected the per-commit prompts to the live syntax. Per `plans/README.md` §"Authoring conventions" stream RFCs and `scope.md` are not retroactively rewritten; this row is the authoritative on-disk record so future drivers do not have to re-derive the live shape from the source. m4 retrospective §2.7. | ratified | — |
| 46 | 2026-05-11 | **`env.allow_secrets: Vec<String>` is an m5a manifest extension on `[capabilities.<bundle>.env]`** that lists env-var names the scrubber honours without `flags.i_know_what_im_doing`. Pairs with `env.pass`: only names appearing in a matching `env.pass` entry are forwarded to the spawned plugin; unused `allow_secrets` entries emit a plain stderr warning at install (`warning: unused allow_secrets entry '<name>' (no matching env.pass entry)`) and an `install_accepted` audit-payload entry (m5a c27). `rfl status` shows accepted entries with a TTY-yellow `explicit secret: <names>` suffix (non-TTY `[SECRET: <names>]`) per c28. Mutually exclusive at install with `flags.i_know_what_im_doing` for any given env name. | The round-2 m5a fallback (force every bundled provider to use `i_know_what_im_doing`) made the demo-bar walkthrough hostile to first-time users: a fresh operator running the bundled `rfl-openai` would hit the red `[OVERRIDE]` marker for a routine API-key forward. `env.allow_secrets` makes the consent explicit at the manifest layer (plugin author declares which secret names they need) and shifts the `rfl status` marker from red `[OVERRIDE]` to yellow `explicit secret: <names>`. Net-new in m5a; no prior `decisions.md` row anchor (the choice is additive to the existing env-scrubbing surface, not a refinement of a prior decision). m5a retrospective §7.1. | ratified | — |
| 47 | 2026-05-11 | **`bindings.tool_meta.<tool>.grant_match` is a JSON-Schema *template* contract**, not a per-tool-call schema validator. At `/grant` time, the slash handler compiles the user's matcher template against the schema (one-shot validation, the compiled validator is dropped); runtime matching of a `tool_request` against the stored `GrantMatcher::Structural { template }` is structural-subset comparison. The slash handler's default-plugin resolution uses `BrokerAcl::tool_route(tool)` (the dispatch-target table) rather than `session.tool_owner` (which is empty without a conflict). | The shape-contract framing keeps the matching algorithm O(structural-walk) at the gate's hot path while still catching obvious template-shape errors (e.g. `/grant send-mail to="alice"` when `to` is `array<string>`) at template-compile time. Full per-call schema validation would duplicate the existing OpenRPC parameter check and cost a validator allocation on every tool dispatch. Net-new in m5a; no prior `decisions.md` row anchor. m5a retrospective §7.2. | ratified | — |
| 48 | 2026-05-11 | **The m5 roadmap row splits across m5a (sinks + confirmation + grants + bundled provider + install-time refusal + three of the four roadmap negatives) and m5b (taint propagation + plugin-supplied-taint superset check + the verbatim exfil negative + the `rafaello-fetch` fixture).** Pre-authorised by the m5 roadmap row's "May split…" language in `milestones/README.md`. m5b inherits the gate, `ConfirmState`, `UserGrants`, slash handler, TUI overlay, audit log, `core.tools_list` + `CorePluginService`, `rfl-openai`, broker outstanding-dispatched map, and install-time trifecta refusal. | The taint primitives are a self-contained surface that pairs naturally with the verbatim demo; bundling them with m5a would have grown the milestone past the m3 22-pi-round threshold. The m5a scope landed at 6 pi rounds against the m4 baseline, supporting the split as the right line. Net-new in m5a; refines the m5 roadmap row in `milestones/README.md`. m5a retrospective §7.3. | ratified | — |
| 49 | 2026-05-11 | **`core.tools_list` is a fittings RPC served by `CorePluginService` registered as a fittings peer before the spawn loop.** The supervisor builds a `ToolSchemaCatalog` from every installed plugin's OpenRPC `methods` × `provides.tools` intersection at `run_chat` startup; `CorePluginService` owns the catalog. Bundled `rfl-openai` calls `core.tools_list` after its own fittings handshake, caches the result, and forwards the schemas as the `tools` field of every `ChatCompletionRequest`. Failure to build the catalog fails `run_chat` startup before any plugin spawns; failure of the plugin-side `core.tools_list` call is fatal-exit for the plugin (m5a c36). | Centralising the tool-schema catalog at the supervisor lets every provider see a consistent view without each plugin re-discovering the lock. Failing closed at startup keeps the no-tools-leaked-after-misconfiguration invariant: if the catalog can't build, no provider can take a turn. Net-new in m5a; no prior `decisions.md` row anchor. m5a retrospective §7.4. | ratified | — |
