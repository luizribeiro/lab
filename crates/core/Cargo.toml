[package]
name = "capsa-core"
version = "0.1.0"
edition = "2021"

[build-dependencies]
pkg-config = "0.3"

[dependencies]
anyhow = "1.0"
capsa-sandbox = { path = "../sandbox" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
