name: lockin

on:
  push:
    branches: [main]
    paths:
      - "lockin/**"
      - "outpost/**"
      - "shared/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/lockin.yml"
  pull_request:
    paths:
      - "lockin/**"
      - "outpost/**"
      - "shared/**"
      - "flake.nix"
      - "flake.lock"
      - ".github/workflows/lockin.yml"

permissions:
  id-token: write
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: DeterminateSystems/nix-installer-action@v16
        with:
          determinate: true

      - uses: DeterminateSystems/magic-nix-cache-action@c6a5746789488c8753af0d61fc6b5d79a80391f3
        with:
          use-flakehub: false

      - name: Run tests
        run: |
          nix develop .#lockin --impure -L --command \
            cargo test --manifest-path lockin/Cargo.toml --workspace &&
          nix develop .#lockin --impure -L --command \
            cargo test --manifest-path lockin/Cargo.toml -p lockin --features tokio --test tokio

      - name: Run nix checks
        run: |
          system=$(nix eval --impure --raw --expr 'builtins.currentSystem')
          nix build -L ".#checks.${system}.lockin-wrap-tests"
